# =============================================================================
# AI Companion - Environment Configuration
# =============================================================================
# Copy this file to .env and update with your actual values.
# NEVER commit .env to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# LLM Provider Configuration
# -----------------------------------------------------------------------------
# Active provider format: provider/model (e.g., ollama/llama3.2, openai/gpt-4o)
# Supported providers: ollama, lmstudio, openai, anthropic
ACTIVE_PROVIDER=ollama/llama3.2

# Cloud API Keys (REQUIRED if using cloud providers)
# Leave empty or remove if only using local providers
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Local Provider URLs
OLLAMA_BASE_URL=http://localhost:11434
LM_STUDIO_URL=http://localhost:1234

# -----------------------------------------------------------------------------
# TTS Server Configuration
# -----------------------------------------------------------------------------
TTS_HOST=localhost
TTS_PORT=3000
TTS_VOICE=en-US-AriaNeural

# TTS Engine: 'coqui-xtts' (offline+cloning), 'piper' (offline), 'edge-tts' (online)
# Engine is auto-selected based on installed packages
TTS_ENGINE=auto

# -----------------------------------------------------------------------------
# Security Configuration (CRITICAL - REQUIRED FOR STARTUP)
# -----------------------------------------------------------------------------
# ⚠️  JWT_SECRET is MANDATORY - Application will NOT start without it!
# 
# Generate a secure secret with:
#   python -c "import secrets; print(secrets.token_hex(32))"
#
# MINIMUM 32 characters required (64 hex characters recommended)
JWT_SECRET=

# CORS - Strictly enforced origins (comma-separated)
# Only http://localhost:5173 and tauri://localhost are allowed
ALLOWED_ORIGINS=http://localhost:5173,tauri://localhost

# Rate Limiting
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=60

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------
APP_ENVIRONMENT=development
APP_PORT=3000
APP_DEBUG=false
LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
DATABASE_URL=sqlite:///./data/companion.db

# -----------------------------------------------------------------------------
# Voice Cloning (Optional)
# -----------------------------------------------------------------------------
# Voice cloning requires Coqui TTS: pip install TTS
# Cloned voice profiles are stored in ./data/speaker_profiles/
# Reference audio files for cloning should be 3-30 seconds of clear speech
